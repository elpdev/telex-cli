version: 2

project_name: telex

before:
  hooks:
    - go mod tidy

builds:
  - id: telex
    main: ./cmd/telex
    binary: telex
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: checksums.txt

brews:
  - name: telex
    ids: [default]
    directory: Formula
    repository:
      owner: elpdev
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "chore: update telex formula to {{ .Tag }}"
    homepage: https://github.com/elpdev/telex-cli
    description: "Terminal UI and CLI for telex.sh email workflows"
    license: MIT
    install: |
      bin.install "telex"

aurs:
  - name: telex-cli-bin
    ids: [default]
    homepage: https://github.com/elpdev/telex-cli
    description: "Terminal UI and CLI for telex.sh email workflows"
    maintainers: ["elpdev <noreply@github.com>"]
    license: MIT
    private_key: "{{ .Env.AUR_KEY }}"
    git_url: "ssh://aur@aur.archlinux.org/telex-cli-bin.git"
    skip_upload: auto
    package: |-
      install -Dm755 telex "${pkgdir}/usr/bin/telex"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
